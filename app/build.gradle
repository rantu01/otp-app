plugins {
    id("com.android.application")
}

android {
    namespace = "com.otpfetch.app"
    compileSdk = 34

    def signingEnv = new Properties()
    def signingEnvFile = file("$rootDir/.env")
    if (signingEnvFile.exists()) {
        signingEnvFile.withInputStream { signingEnv.load(it) }
    }
    def signingValue = { String name ->
        def value = System.getenv(name)
        return value != null && !value.isEmpty() ? value : signingEnv.getProperty(name, "")
    }

    defaultConfig {
        applicationId = "com.otpfetch.app"
        minSdk = 26
        targetSdk = 34
        // Increase this for every published APK. Android uses it to decide
        // whether an APK is a valid update of the installed app.
        versionCode = 2
        versionName = "1.1"
    }

    signingConfigs {
        release {
            def keystorePath = signingValue("ANDROID_KEYSTORE_PATH")
            if (keystorePath != null && !keystorePath.isEmpty()) {
                storeFile = file(keystorePath)
                storePassword = signingValue("ANDROID_KEYSTORE_PASSWORD")
                keyAlias = signingValue("ANDROID_KEY_ALIAS")
                keyPassword = signingValue("ANDROID_KEY_PASSWORD")
            }
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            signingConfig signingConfigs.release
            proguardFiles(
                getDefaultProguardFile("proguard-android-optimize.txt"),
                "proguard-rules.pro"
            )
        }
    }
    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }
}

dependencies {
    implementation("androidx.appcompat:appcompat:1.7.0")
    implementation("com.google.android.material:material:1.12.0")
    implementation("androidx.core:core:1.13.1")
}
