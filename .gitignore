# Android / Gradle
*.iml
.gradle/
local.properties
.idea/
.DS_Store
build/
app/build/
app/release/

# Signing secrets — never commit these
*.jks
*.keystore
.env

# Wrapper jar (we build with system Gradle in CI, see .github/workflows/)
gradle/wrapper/gradle-wrapper.jar

# Shared backend (Node)
backend/node_modules/
backend/db.json
backend/.env

# Admin app module build outputs
admin-app/build/
