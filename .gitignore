# Android / Gradle
*.iml
.gradle/
local.properties
.idea/
.DS_Store
build/
app/build/
app/release/

# Signing secrets — never commit these
*.jks
*.keystore

# Wrapper jar (we build with system Gradle in CI, see .github/workflows/)
gradle/wrapper/gradle-wrapper.jar
